# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/

# Generated results
results/

# Downloaded / generated data at repo root (keep folder structure + docs only).
# Scoped with a leading slash so it does NOT match frontend/public/data (the
# fixed dataset that must ship with the static site).
/data/**
!/data/README.md
!/data/*/
!/data/**/.gitkeep

# OS / editor
.DS_Store
